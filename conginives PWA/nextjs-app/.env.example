# Keep true while teammates are still wiring DB.
USE_MOCK_DATA=true

# Set this when the DB is ready, then run Prisma migrations.
DATABASE_URL="postgresql://username:password@localhost:5432/cognivus?schema=public"
